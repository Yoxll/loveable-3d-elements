# components/clouds

Cloud systems. The production-ready package lives in `/volumetric-clouds` (do not modify); new experiments go here.

Drop React Three Fiber (`.tsx`) components here. Each component should be self-contained and import its assets from `/assets`.
