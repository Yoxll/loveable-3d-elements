# components/terrain

Heightfield/terrain meshes, displacement and ground shading components.

Drop React Three Fiber (`.tsx`) components here. Each component should be self-contained and import its assets from `/assets`.
