# examples

Minimal, runnable demo scenes showing how to combine components and assets.

Each example should be a single folder with its own `README.md` explaining what it demonstrates and which components/assets it uses.
